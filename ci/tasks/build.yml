platform: linux

image_resource:
  type: docker-image
  source:
    repository: node
    tag: latest

inputs:
  - name: source-code

outputs:
  - name: source-code

run:
  path: sh
  args:
  - -exc
  - |
    ls source-code
    echo "--------"
    ls source-code-built
